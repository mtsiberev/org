@model OrganizationsWebApplication.Models.DepartmentModel

@{
    ViewBag.Title = "DepartmentInfo";
}
<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="../../Content/style.css" type="text/css" />
</head>
<body>
    <table>
        <thead>
            <tr>
                <td class="block" style="background: #3F51B5; width: 400px">
                    <span style="color: #fff">
                        Department @Model.Name
                    </span>
                </td>
                
                <td class="operationBlock" style="background: #3F51B5">
                    @using (Html.BeginForm("Index", "Home", FormMethod.Post))
                    {
                        <input type="image" src="../../Content/ic_home_24px_inverse.svg" alt="add" name="submit" />
                    }
                </td>
            </tr>
        </thead>

        <tbody>
            @foreach (var employee in Model.Employees)
            {
                <tr>
                    <td class="block" style="color:black">
                        <div>@employee.Name</div>
                    </td>
                    <td class="operationBlock">
                        @using (Html.BeginForm("UpdateEmployeeMenu", "Employee",
                            new { id = employee.Id }))
                        {
                            <input type="image" src="../../Content/ic_mode_edit_24px.svg" alt="edit" name="submit" />
                        }
                    </td>

                    <td class="operationBlock">
                        @using (Html.BeginForm("DeleteEmployee", "Employee",
                            new { id = employee.Id }))
                        {
                            <input type="image" src="../../Content/ic_delete_24px.svg" alt="delete" name="submit" />
                        }
                    </td>

                </tr>
            }
        </tbody>
    </table>

    <p></p>
    @using (Html.BeginForm("AddEmployeeMenu", "Employee",
        new { id = @Model.Id }, FormMethod.Post))
    {
        <button type="submit">Add employee</button>
    }

</body>
</html>
