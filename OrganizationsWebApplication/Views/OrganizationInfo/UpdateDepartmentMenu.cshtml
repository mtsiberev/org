@{
    ViewBag.Title = "Department";
}

@model OrganizationsWebApplication.Models.EntitiesModels.DepartmentViewModel

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" href="../../Content/style.css" type="text/css" />
</head>
<body>

    <table>
        <tr>
            <td class="block" style="background: #3F51B5; width: 400px">
                <span style="color: #fff">
                    Please update department @Model.Name name:
                </span>
            </td>
        </tr>
    </table>

    @using (Html.BeginForm("UpdateDepartment", "OrganizationInfo"))
    {
        <table>
            <tr>
                <td>
                    <div class="container">
                        <div class="group">
                            @Html.TextBoxFor(m => m.Name, new { @class = "inputMaterial", @type = "text", @required = "required" })
                            @Html.HiddenFor(m => m.Id)
                            @Html.HiddenFor(m => m.ParentId)
                            @Html.Hidden("pageNumberInOrganizationInfo")
                           @Html.Hidden("sortType")
                            <span class="highlight"></span>
                            <span class="bar"></span>
                            <label>Name</label>
                            <span style="bottom: 35px; left: 65%; position:relative; ">
                                <input type="image" src="../../Content/ic_done_24px.svg" alt="Update" name="submit" />
                            </span>
                        </div>
                    </div>
                </td>
            </tr>
        </table>
    }

</body>
</html>